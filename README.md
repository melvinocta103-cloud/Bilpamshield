# Bilpamshield

## Environment variables

Copy `.env.example` to `.env` for local development. In Netlify, add these variables under **Site configuration > Environment variables** before deploying:

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
